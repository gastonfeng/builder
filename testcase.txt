** Settings ***
Documentation  Test of all elements in OpenERP
Resource       odoo-robot-framework/odoo_10_0.robot
Library        odoo-robot-framework/connection_erp.py

*** Test Cases ***
登陆
    Login     ${ODOO_DB}      ${USER}     ${PASSWORD}

安装模块
	${module_installed}=	Install Module  ${ODOO_URL_DB}      ${ODOO_DB}      ${USER}     ${PASSWORD}    builder
	log to console	${module_installed}
	Run Keyword Unless	${module_installed}	Fail

更新模块
	${module_upgraded}=	Upgrade Module  ${ODOO_URL_DB}      ${ODOO_DB}      ${USER}     ${PASSWORD}    builder
	log to console	${module_upgraded}
	Run Keyword Unless	${module_upgraded}	Fail

进主菜单

   MainMenuXMLid    builder.XML_ID_MENU_builder_ROOT
菜单检查

    SubSubMenuXMLid    builder.menu_act_kaikong_builder_system
    SubSubMenuXMLid    builder.menu_act_kaikong_builder_devices
    SubSubMenuXMLid    builder.menu_act_kaikong_builder_device_varis
    SubSubMenuXMLid    builder.menu_act_kaikong_builder_device_template
    SubSubMenuXMLid    builder.menu_act_kaikong_builder_device_template_vari

close
    close browser